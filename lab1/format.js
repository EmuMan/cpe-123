class Vector3 {

   constructor(x, y, z) {
      this.x = x;
      this.y = y;
      this.z = z;
    }

}

let rain;

function setup() {
   createCanvas(710, 400, WEBGL);
   rain = [];
 }
 
 function draw() {
    
   background(0);
   
   ambientLight(200, 200, 200);

   noStroke();

   //%%code%%

 }
