class Vector3 {

   constructor(x, y, z) {
      this.x = x;
      this.y = y;
      this.z = z;
    }

}

let rain;

function setup() {
   createCanvas(710, 400, WEBGL);
   rain = [];
 }
 
 function draw() {

   background(80, 80, 120);
   
   ambientLight(40, 40, 40);

   noStroke();

   //%%code%%

 }
